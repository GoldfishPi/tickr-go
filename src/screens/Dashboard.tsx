import React from 'react';
import { 
    Layout,
    Text
} from '@ui-kitten/components';

export const DashboardScreen = () => {
    return (
        <Layout style={{ flex:1 }}>
            <Text> Dashboard Screen Works!</Text>
        </Layout>
    );
}
